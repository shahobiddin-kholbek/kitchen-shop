export type AdminType = {
    id: string
    name: string
    lastName: string
    email: string
    password: string
    confirmPassword: string
    phone: string
    accessLevel: string
}

export type SignInAdmin = {
    email: '',
    password: ''
}